export interface KeywordData {
    id: number;
    keyword: string;
};

export interface HistoryData {
    id: number;
    keyword: string;
    date: Date;
};

export interface ProductData {
    id: number;
    name: string;
    imageUrl: string;
};